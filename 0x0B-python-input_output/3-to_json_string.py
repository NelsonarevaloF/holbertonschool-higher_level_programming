#!/usr/bin/python3
"""here it's the function to_json_string"""

import json


def to_json_string(my_obj):
    """return the JSON representation of an object (string)"""
    return (json.dumps(my_obj))
